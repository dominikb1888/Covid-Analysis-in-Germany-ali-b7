{
    "name": "FastAPI Development",
    "build": {
        "dockerfile": "../Dockerfile",
        "context": ".."
    },
    "forwardPorts": [8000],
    "settings": { 
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": ["ms-python.python"],
    "postStartCommand": "echo 'Container is ready!'"
}
